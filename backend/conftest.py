"""Pytest bootstrap.

Test configuration (in-memory SQLite + fast password hasher) lives in
``config.settings_test``, selected via ``DJANGO_SETTINGS_MODULE`` in
``pyproject.toml``. This file is intentionally minimal.
"""
